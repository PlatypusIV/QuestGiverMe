import { combineReducers } from 'redux';

const viewReducer = (state, action) => {
	let newState = { ...state };
	try {
		switch (action.type) {
			case '':
				break;
			default:
				break;
		}
	} catch (error) {
		console.log(error);
	}
};

export const rootReducer = () => {};
